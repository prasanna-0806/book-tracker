<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <link rel="stylesheet" href="/styles.css">
  <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;600&family=Playfair+Display:wght@700&display=swap" rel="stylesheet">

  <title>Edit Book</title>
</head>
<body>
 <main>
     <h1>Edit Book</h1>

  <form action="/edit/<%= book.id %>" method="POST">
    <label>Title:</label><br>
    <input type="text" name="title" value="<%= book.title %>" required><br><br>

    <label>Author:</label><br>
    <input type="text" name="author" value="<%= book.author %>" required><br><br>

    <label>ISBN:</label><br>
    <input type="text" name="isbn" value="<%= book.isbn %>"><br><br>

    <label>Rating:</label><br>
    <input type="number" name="rating" min="1" max="5" value="<%= book.rating %>"><br><br>

    <label>Notes:</label><br>
    <textarea name="notes" rows="4" cols="50"><%= book.notes %></textarea><br><br>

    <label>Date Read:</label><br>
    <input type="date" name="date_read" value="<%= book.date_read ? book.date_read.toISOString().slice(0, 10) : '' %>"><br><br>

    <button type="submit">Update Book</button>
  </form>

  <p><a href="/">← Back to Home</a></p>
  </main>
</body>
</html>
